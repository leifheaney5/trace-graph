# Contributing

Create feature branches from `main` using `feat/*`, `fix/*`, `docs/*`, `test/*`, or `chore/*`. Keep commits focused and use Conventional Commit messages. Run `npm run lint` and `npm run build` before opening a pull request. Keep synthetic demo data free of proprietary information.
